/// Requires that a and b both allow <>== operators
pub fn <T> max(a: T, b: T) T {
    return a >= b ? a : b;
}

/// Requires that a and b both allow <>== operators
pub fn <T> min(a: T, b: T) T {
    return a <= b ? a : b;
}

pub fn max_i64(a: i64, b: i64) i64 {
    return max::<i64>(a, b);
}